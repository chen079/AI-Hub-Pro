
# 🤖 AI Hub Pro

<div align="center">

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Python](https://img.shields.io/badge/python-3.8+-green.svg)
![Vue](https://img.shields.io/badge/vue-3.x-emerald.svg)
![Status](https://img.shields.io/badge/status-active-orange.svg)

[English](#-english) | [中文文档](#-中文)

</div>

---

<a name="-english"></a>
## 🇬🇧 English

**AI Hub Pro** is a lightweight yet powerful AI model aggregation client built with **Flask** and **Vue 3**. It is designed to provide a smooth, ChatGPT-like experience while supporting any OpenAI-compatible API (e.g., OneAPI, NewAPI, DeepSeek, Claude, Midjourney, etc.).

### ✨ Key Features

- **🚀 Universal Model Support**: Perfectly adapts to GPT-4o, Claude 3.5, Gemini, DeepSeek, and various open-source models.
- **🎨 Multimodal Rendering**:
  - **Images**: Previews for images generated by Midjourney, Dall-E, Flux, etc.
  - **Video**: Native player support for Luma, Runway, Sora, and Kling generations.
  - **Audio**: Audio player support for Suno and Udio.
- **🧠 Deep Thinking Mode**: accurately parses and collapses the `<think>` tags, providing a native experience for reasoning models like **DeepSeek R1** and **Claude Thinking**.
- **⚡ Stream Response**: Fast, typewriter-style streaming text generation.
- **🛠️ Connection Testing**: Built-in **"Test Connection"** feature to instantly verify your API Endpoint and Key validity.
- **🔐 Privacy & Security**:
  - API Keys are encrypted using Fernet (symmetric encryption) and stored locally in SQLite.
  - Multi-user support with isolated settings for each account.
- **📂 Document Parsing**: Supports uploading PDF, Word (.docx), and TXT files for RAG-like context injection.
- **xxxx Dark Mode**: Built-in light/dark themes that adapt to system settings or manual toggles.
- **📱 Responsive Design**: Fully optimized for both desktop and mobile devices.

### 🛠️ Tech Stack

- **Backend**: Python (Flask), SQLite (SQLAlchemy), HTTPX (Async/Sync requests), Cryptography.
- **Frontend**: Vue.js 3 (CDN), Tailwind CSS, Marked.js (Markdown parsing), Highlight.js.

### 📦 Quick Start

#### 1. Clone the repository
```bash
git clone https://github.com/yourusername/ai-hub-pro.git
cd ai-hub-pro
```

#### 2. Set up the environment
It is recommended to use a virtual environment:
```bash
# Create virtual environment
python -m venv venv

# Activate (Windows)
venv\Scripts\activate

# Activate (Mac/Linux)
source venv/bin/activate
```

#### 3. Install dependencies
```bash
pip install -r requirements.txt
```

#### 4. Configure Secret Key
In `app.py`, update the secret key configuration for security:
```python
# app.py
app.config['SECRET_KEY'] = 'change-this-to-a-secure-random-key' 
```

#### 5. Run the application
```bash
python app.py
```
Open your browser and visit: `http://127.0.0.1:5000`

#### "run.bat"
we create a script, you can start "run.bat" to use the app directly.

### 📖 Usage Guide

1. **Register/Login**: Click "Create Account" on the login page.
2. **Configure API**:
   - Go to Settings (Avatar -> System Settings).
   - Enter your API Endpoint (e.g., `https://api.openai.com/v1`).
   - Enter your API Key.
   - Click **"Test Connection"** to verify.
   - Click **Save**.
3. **Chat**: Select a model and start chatting. Use the attachment icon to upload files.

---

<a name="-中文"></a>
## 🇨🇳 中文

**AI Hub Pro** 是一个轻量级、功能强大的 AI 模型聚合客户端。基于 **Flask** + **Vue 3** 构建，旨在提供类似 ChatGPT 的流畅体验，同时支持任意兼容 OpenAI 格式的 API（如 OneAPI、NewAPI、DeepSeek、Claude、Midjourney 等）。

### ✨ 核心特性

- **🚀 全模型支持**：完美适配 GPT-4o, Claude 3.5, Gemini, DeepSeek 以及各类开源模型。
- **🎨 多模态渲染**：
  - **图片**：支持 Midjourney/Dall-E/Flux 生成的图片预览。
  - **视频**：支持 Luma/Runway/Sora/Kling 生成的视频直接播放。
  - **音频**：支持 Suno/Udio 生成的音乐播放。
- **🧠 深度思考模式**：完美解析并折叠显示 `<think>` 标签，原生适配 **DeepSeek R1** 和 **Claude Thinking** 推理模型。
- **⚡ 流式响应**：极速打字机效果，拒绝等待。
- **🛠️ 连接测试**：内置 **“测试连接”** 功能，一键验证 API Endpoint 和 Key 是否有效，拒绝配置盲猜。
- **🔐 隐私安全**：
  - API Key 使用 Fernet 对称加密算法本地存储在 SQLite 数据库中。
  - 支持多用户登录，每个用户的设置和会话完全隔离。
- **📂 文档解析**：支持上传 PDF、Word (.docx)、TXT 文件，自动提取文本与 AI 对话。
- **xxxx 暗黑模式**：内置明亮/暗黑主题，支持跟随系统或手动切换。
- **📱 响应式设计**：完美适配桌面端和移动端显示。

### 🛠️ 技术栈

- **后端**：Python (Flask), SQLite (SQLAlchemy), HTTPX (异步/同步请求), Cryptography (加密).
- **前端**：Vue.js 3 (CDN引入), Tailwind CSS, Marked.js (Markdown解析), Highlight.js (代码高亮).

### 📦 快速开始

#### 1. 克隆项目
```bash
git clone https://github.com/yourusername/ai-hub-pro.git
cd ai-hub-pro
```

#### 2. 环境配置
建议使用 Python 虚拟环境：
```bash
# 创建虚拟环境
python -m venv venv

# 激活环境 (Windows)
venv\Scripts\activate

# 激活环境 (Mac/Linux)
source venv/bin/activate
```

#### 3. 安装依赖
```bash
pip install -r requirements.txt
```

#### 4. 配置密钥
在 `app.py` 中，找到配置区域。**注意：在生产环境中，请务必修改 SECRET_KEY**。
```python
# app.py
app.config['SECRET_KEY'] = '请修改这里为一个随机字符串' 
```

#### 5. 运行应用
```bash
python app.py
```
访问浏览器：`http://127.0.0.1:5000`

#### plus: run.bat
我们创建了运行脚本 run.bat，只需要双击即可引导你部署该应用。

### 📖 使用指南

1. **注册/登录**：首次使用请直接在登录页点击“创建新账号”。
2. **配置 API**：
   - 点击左下角头像 -> 打开 **系统设置**。
   - 输入你的 API Endpoint (例如 `https://api.openai.com/v1` 或中转地址)。
   - 输入 API Key。
   - 点击 **“测试连接”** 确保配置正确（新增功能）。
   - 点击 **保存设置**。
3. **开始对话**：选择模型（支持手动输入或从列表中选择），发送消息，或点击附件图标上传文档/图片。

---

## 📄 License / 协议

This project is licensed under the [MIT License](LICENSE).  
本项目基于 [MIT 协议](LICENSE) 开源。
